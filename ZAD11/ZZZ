try {
			cin >> wybor;

			if (wybor > 5 || wybor < 1)

				throw 1;
			
		}

		catch (int) {
			cout << "Blad zakresu" << endl;
		}

		catch (istream::failure) {
			cout << "Blad danych" << endl;
			cin.clear();
			cin.ignore(50, '\n');
		}
